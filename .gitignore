# dependencies
node_modules/

# build output
dist/

# runtime cache (JsonFileCache default path)
.data/

# secrets — plaintext env NEVER committed; only .env.example is tracked
.env
.env.*
!.env.example
!.env.age

# age identities / keys must never be committed
*.key
age-key.txt
keys.txt

# misc
*.log
.DS_Store
.vercel
.env*
